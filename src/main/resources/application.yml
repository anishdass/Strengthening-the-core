server:
  port: 8080

spring:
  application:
    name: springtest
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

logging:
  level:
    org.springframework.security: DEBUG